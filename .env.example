# DAEMON-ONE Environment Configuration
# Copy to .env and modify as needed

# --- App ---
DEBUG=true
SECRET_KEY=django-insecure-change-me-in-production

# --- Production Security (set DEBUG=false first) ---
# Comma-separated list of allowed hosts
ALLOWED_HOSTS=*
# Comma-separated list of trusted origins for CSRF (required for production)
# Example: CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com
CSRF_TRUSTED_ORIGINS=
# Set to false if behind a proxy that handles SSL
SECURE_SSL_REDIRECT=true

# --- Ports (2020-2030 range) ---
DJANGO_PORT=2020
POSTGRES_PORT=2021
REDIS_PORT=2022

# --- Database ---
POSTGRES_DB=daemon_one_db
POSTGRES_USER=daemon_one_user
POSTGRES_PASSWORD=daemon_one_password
POSTGRES_HOST=localhost

# --- Redis ---
REDIS_HOST=localhost

# === External APIs (Only exceptions - no internal API communication) ===

# --- AI Provider (v4.0) ---
# Strategy: HuggingFace (Free) → DeepSeek (Quality/Korean) → OpenRouter (Multi)
AI_PROVIDER=huggingface

# HuggingFace (Free Tier - Primary)
HUGGINGFACE_API_KEY=

# DeepSeek (Quality/Korean - Secondary)
DEEPSEEK_API_KEY=

# OpenRouter (Multi-Model - Tertiary)
OPENROUTER_API_KEY=

# Legacy: Direct Gemini API (use OpenRouter instead)
# GEMINI_API_KEY=

# --- Superuser (Auto-created on first run) ---
DJANGO_SUPERUSER_USERNAME=daemon
DJANGO_SUPERUSER_EMAIL=admin@daemon.local
DJANGO_SUPERUSER_PASSWORD=daemonkorea2026

# --- Stripe (Payments) ---
# STRIPE_PUBLIC_KEY=
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# --- OAuth Providers (Social Login) ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# --- Email (External SMTP) ---
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=

# --- Monitoring & Observability (v4.0) ---
SENTRY_DSN=
LOGFIRE_TOKEN=

# --- Security (v4.0) ---
# Cloudflare Turnstile
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# --- SEO & Marketing (v4.0) ---
SEO_BASE_URL=https://your-daemon-app.com
MARKETING_REFERRAL_SALT=your-referral-salt-here

# --- Backup (v4.0) ---
# S3/R2 Configuration for Database/Media Backups
BACKUP_S3_BUCKET=
BACKUP_S3_ACCESS_KEY=
BACKUP_S3_SECRET_KEY=
BACKUP_S3_ENDPOINT_URL=

# === Architecture Note ===
# This project uses HATEOAS (HTML over the wire)
# - No internal JSON API communication
# - Server returns HTML fragments
# - HTMX fetches HTML, not JSON
# - Only external APIs (Gemini, Stripe, OAuth) use JSON
